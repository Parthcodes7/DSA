#include <bits/stdc++.h>
using namespace std;

void explainpairs(){
    list<int> ls;
    ls.push_back(2);
    ls.emplace_back(4);

    ls.push_front(5);

    ls.emplace_back();{2,4};
}

int main(){
    
}